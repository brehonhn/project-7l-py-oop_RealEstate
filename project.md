در ادامه یک فایل **README.md** حرفه‌ای و استاندارد برای پروژه‌ی OOP مشاور املاک تو نوشته‌ام.
این README کاملاً با ساختار پروژه، لایه‌ها، معماری، نحوه‌ی اجرا، ویژگی‌ها و نحوهٔ توسعه مطابقت دارد.

---

# 📘 **RealEstate OOP Project**

یک پروژهٔ شی‌ءگرای کامل برای مدیریت اطلاعات املاک، شامل ثبت ملک، ثبت فایل فروش/اجاره، جستجو، و مدیریت داده‌ها.
این پروژه به عنوان نمونه‌ای از **طراحی شی‌ءگرا (OOP)** در پایتون توسعه داده شده و شامل مفاهیمی مانند:

* **وراثت (Inheritance)**
* **ترکیب (Composition)**
* **کپسوله‌سازی (Encapsulation)**
* **چندریختی (Polymorphism)**
* **Abstraction + ABC**
* **Repository Layer**
* **Service Layer**
* **Console UI**

---

# 📁 **ساختار پروژه**

```
oop_RealEstate/
│
├── control/
│   └── main.py                # نقطه ورود برنامه
│
├── entity/
│   ├── base/                  # BaseEntity – مدیریت ID و ذخیره داده‌ها
│   ├── property/              # انواع ملک: Apartment, House, Shop, ...
│   ├── seller/                # اطلاعات فروشنده
│   └── listing/               # SaleListing, RentListing
│
├── repository/
│   └── RealEstateRepository.py  # مدیریت Query ها و جستجوها
│
├── service/
│   └── RealEstateService.py     # لایه منو و تعامل با کاربر
│
├── util/
│   └── enums/UsageType.py       # نوع کاربری ملک (مسکونی، اداری، تجاری)
│
├── .gitignore
└── README.md
```

---

# 🏗 **معماری پروژه**

پروژه با معماری چندلایه طراحی شده:

## 1️⃣ **Entity Layer**

تمام مدل‌های دامنه در این لایه قرار دارند:

### ✔ BaseEntity

* تولید ID
* نگهداری همهٔ objectها در حافظه

### ✔ Property (کلاس انتزاعی)

ویژگی‌های مشترک همهٔ املاک:

* فروشنده
* متراژ
* تعداد اتاق
* محله
* آدرس
* سال ساخت
  و…

### ✔ Subclasses:

* **Apartment**
* **House**
* **Shop**

### ✔ Seller

اطلاعات صاحب ملک.

### ✔ Listing

دو نوع فایل:

* SaleListing (قیمت بر متر، تخفیف، معاوضه)
* RentListing (رهن، اجاره، قابلیت تبدیل)

---

## 2️⃣ **Repository Layer**

کلاس `RealEstateRepository` مسئول:

* نگهداری داده‌ها
* استخراج لیست ملک‌ها و فایل‌ها
* جستجو بر اساس فیلدهای مختلف:

  * محله
  * قیمت
  * متراژ
  * تعداد خواب

---

## 3️⃣ **Service Layer**

لایهٔ تعامل با کاربر و مدیریت عملیات برنامه.

### ویژگی‌ها:

* منوی گرافیکی (متنی) با رنگ‌بندی
* انجام عملیات CRUD:

  * ثبت ملک جدید
  * ثبت فایل فروش/اجاره
  * نمایش لیست ملک‌ها و فایل‌ها
  * جستجو
  * حذف و ویرایش

---

# 🎮 **نحوهٔ اجرا**

۱. مخزن را clone کنید:

```bash
git clone https://github.com/your-repo/oop_RealEstate.git
cd oop_RealEstate
```

۲. محیط مجازی بسازید:

```bash
python -m venv .venv
```

۳. فعال‌سازی محیط مجازی:

Windows:

```bash
.venv\Scripts\activate
```

Mac/Linux:

```bash
source .venv/bin/activate
```

۴. اجرای برنامه:

```bash
python control/main.py
```

---

# 🔍 **قابلیت‌های جستجو**

جستجو بر اساس:

### 🔹 فایل فروش

* محله
* قیمت کل
* متراژ
* تعداد اتاق

### 🔹 فایل رهن/اجاره

* محله
* اجاره
* متراژ
* تعداد اتاق

خروجی همیشه **لیستی از Listing Object**‌هاست که قابلیت:

* فیلتر بیشتر
* نمایش
* تبدیل به JSON
  دارند.

---

# 🧪 **Seed Data**

در Repository داده‌های پیش‌فرض ایجاد می‌شود تا از همان ابتدا بتوانید سیستم را تست کنید.

---

# 🧩 **ویژگی‌های شی‌ءگرایی که در پروژه استفاده شده**

| ویژگی                                   | توضیح                                       |
| --------------------------------------- | ------------------------------------------- |
| **Abstraction (کلاس Property انتزاعی)** | جلوگیری از ساخت مستقیم Property             |
| **Inheritance**                         | آپارتمان، خانه، مغازه ← ارث‌بری از Property |
| **Polymorphism**                        | Listing ها متدهای متفاوت دارند              |
| **Encapsulation**                       | ویژگی‌های private و getter/setter           |
| **Composition**                         | ملک شامل District و Seller است              |
| **Repository Pattern**                  | مدیریت داده از طریق یک لایه جدا             |

---

# 🚀 **آینده توسعه پروژه**

موارد پیشنهادی برای نسخه‌های بعدی:

* ذخیره‌سازی داده‌ها در فایل JSON یا دیتابیس SQLite
* رابط کاربری گرافیکی (Tkinter یا PyQt)
* API با FastAPI
* تست واحد با pytest
* لاگ‌گیری حرفه‌ای

---

# 📝 **لایسنس**

می‌توانید آزادانه از این پروژه برای یادگیری یا توسعه شخصی استفاده کنید.

---
